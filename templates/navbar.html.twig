<nav class="navbar navbar-expand-md navbar-dark bg-dark bg-secondary fixed-top">
    <div class="w-100 text-center">
        <a class="navbar-brand font-weight-bold" href="{{ path('app_index') }}">The Player</a>
    </div>
    <div class="position-absolute end-0">
        <a type="button" href="{{ path('app_admin') }}" class="btn btn-warning">Admin</a>
    </div>
</nav>
